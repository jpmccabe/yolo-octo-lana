package model;

// Represents all of the operation in the language.
public enum Operation {
    RIGHT_SHIFT_POINTER, LEFT_SHIFT_POINTER, INCREMENT_POINTER,
    DECREMENT_POINTER, PRINT, LOOP_BEGIN, LOOP_END;
}
