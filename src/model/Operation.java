package model;

// Represents all of the operation in the language.
public enum Operation {
    INCREMENT_POINTER, DECREMENT_POINTER, INCREMENT_DATA,
    DECREMENT_DATA, PRINT, LOOP_BEGIN, LOOP_END;
}
